{
  "scripts": {
    "dev": "parcel ./src/index.html",
    "build": "rm -rf ./dist && parcel build src/index.html",
    "clean": "rm -rf ./dist .parcel-cache"
  },
  "devDependencies": {
    "parcel": "^2.8.2"
  }
}
